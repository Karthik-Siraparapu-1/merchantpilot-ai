# Infrastructure

Local infrastructure is defined by the repository-root `docker-compose.yml`. It provides PostgreSQL and Redis only; deployable application manifests and infrastructure-as-code are intentionally introduced with the deployment phase.
