{
  "name": "@merchantpilot/ai-service",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "python3 -m uvicorn merchantpilot_ai.main:app --app-dir src --port 8000 --reload",
    "build": "python3 -m compileall -q src",
    "lint": "python3 -m compileall -q src",
    "typecheck": "python3 -m compileall -q src",
    "test": "python3 -m compileall -q src"
  }
}
